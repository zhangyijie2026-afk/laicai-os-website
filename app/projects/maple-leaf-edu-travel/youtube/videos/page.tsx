import { YoutubeWorkbenchShell } from "@/components/youtube-workbench-shell";
import { periodSummary, videos, youtubeSummary } from "@/content/youtube-workbench";
import { createMetadata } from "@/lib/metadata";

export const metadata = createMetadata("YouTube Videos — Project 001", "Video-level YouTube performance for Project 001.", "/projects/maple-leaf-edu-travel/youtube/videos");
export default function YoutubeVideos() { return <YoutubeWorkbenchShell active="Videos" eyebrow="CONTENT LIBRARY" title="Every video should teach the system something." copy="Current public-content views with 28-day watch metrics where YouTube Studio exposes them."><section className="dashboard-panel table-panel"><div className="panel-heading"><div><span>ALL CONTENT</span><h2>{youtubeSummary.publishedVideos} published assets</h2></div><small>{youtubeSummary.shorts} Shorts · {youtubeSummary.longVideos} videos · {periodSummary.label}</small></div><div className="video-table"><div className="video-row video-table-head"><span>Video</span><span>Lifetime views</span><span>Avg watch</span><span>Avg viewed</span><span>Action</span></div>{videos.map((video) => <article className="video-row" key={video.id}><div><small>{video.format} · {video.published} · {video.periodViews.toLocaleString()} views in period</small><strong>{video.title}</strong></div><b>{video.views.toLocaleString()}</b><span>{video.averageWatch}</span><span>{video.averageViewed}</span><em className={video.tone}>{video.status}</em></article>)}</div></section><p className="data-footnote">“—” means the metric was not visible in the selected Studio report; it has not been estimated.</p></YoutubeWorkbenchShell>; }

